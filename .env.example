# QueryMind example environment file
# Do NOT commit secrets. Copy to .env and fill real values locally or in CI.

# PostgreSQL connection used by backend and agent (use service name 'db' in Docker)
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@db:5432/ai_analytics_db
DB_USER=querymind_admin
DB_PASSWORD=changeme

# JWT / Auth
SECRET_KEY=REPLACE_ME
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Nginx / Docker
# Format: host_port:container_port
NGINX_DOCKER_PORT=8080:80
DOMAIN_NAME=localhost

# LLM provider keys (fill only on your deployment environment)
GROQ_API_KEY=

# Optional: Development test user (create a permanent login at backend startup)
# Set both `DEV_TEST_USERNAME` and `DEV_TEST_PASSWORD` in your local .env to enable auto-creation.
# Example (do NOT commit real credentials):
# DEV_TEST_USERNAME=ilhaam_and_mohsin
# DEV_TEST_PASSWORD=your_secret_password_here
# DEV_TEST_FULLNAME=Ilhaam & Mohsin
# DEV_TEST_EMAIL=ilhaam_and_mohsin@example.com

# Notes:
# - Replace placeholders above with real values in a local .env (never commit real .env).
# - If your friend provided a .env, merge non-conflicting keys into your local .env only.
